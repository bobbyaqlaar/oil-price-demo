"""
examples/oil-price-agent/workflows/ — Domain Temporal workflow for this tenant.

This is what a TENANT repo's own workflow directory looks like (§25, §28).
It is built on the pattern in runtime/workflows/base_workflow.py but is NOT
part of the framework — copy this whole examples/oil-price-agent/ directory
into your own repository and adapt it; never deploy from
AgentSmith/examples directly.
"""
